pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
		mvn clean compile test-compile
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
